# Railway / production — задай в Dashboard → Variables
ADMIN_TOKEN=change-me-admin
WEBHOOK_SECRET=change-me-webhook
CORS_ORIGIN=https://thisviolethydra.github.io
DB_PATH=/data/showcase.db
# PORT — Railway подставляет сам
