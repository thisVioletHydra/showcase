# Figma references

Visual references for the Home V3 layout (from TZ / design export):

- `home-full.jpg` — full vitrina composition
- `catalog-open.png` — catalog dropdown with sidebar + 5 columns

Place exported screenshots here when available. The implemented UI follows `figma/tokens.css` and semantic flex/grid layout.
